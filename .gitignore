node_modules/
__pycache__/
.pytest_cache/
mock/cupel-mock.sqlite
# WAL sidecars (mock/db.py enables journal_mode=WAL for P2-PERSIST)
*.sqlite-wal
*.sqlite-shm
litestream.yml
dist/
coverage/
test-results/
playwright-report/
blob-report/
# `npm run init` keeps the pre-rewrite config next to it
agentic.config.ts.bak
agentic.config.ts.tmp
# Nothing in this repo reads a .env — every secret is an env var set by the
# deploy (docs/deployment.md) — but the repo is public, so the file that would
# hold one must never be committable by accident.
.env
.env.*
!.env.example
